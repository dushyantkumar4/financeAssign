# financeAssign

tech stack => nodejs , express js , mongodb , jwt 
after clone the repo run the command npm install 
run the command to start npm run dev /or node src/server.js
i try to keep the project simple & build a expense tracker where 
first need to register that will send a token to frontend as well we can save it into the bearer tokan & same with login
keep the 3 roles Viewer, Analyst,Admin => viewer can only see the no of users are using the plateform & active users no. only , Analyst can create , update , delete, view the it's own finace & self data,
Admin will be the top of the hierarchy who can manage all the users & finance 

